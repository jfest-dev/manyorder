/**
 * Fallback wording shown to customers at checkout when a store hasn't set a flat
 * delivery fee (the "to be confirmed" case) and hasn't written its own message.
 * Shared by the merchant Delivery screen (as the placeholder/preview default) and
 * the storefront checkout, so the two never drift.
 */
export const DEFAULT_DELIVERY_TBC_MESSAGE =
  'The delivery fee will be confirmed by the seller over WhatsApp.';
